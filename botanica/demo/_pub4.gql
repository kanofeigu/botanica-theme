query {
  productByHandle(handle: "monstera-deliciosa") {
    title
    status
    publishedOnCurrentChannel
    resourcePublicationsV2(first: 3) { nodes { isPublished publication { id name } } }
  }
}
